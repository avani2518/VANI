Clean and structure the given OCR text into JSON.

Rules:
- Extract only clearly identifiable fields
- Fix OCR errors (spacing, merged words, broken text)
- Normalize:
  - Names → proper spacing and capitalization
  - Dates → DD/MM/YYYY
  - Addresses → readable format
- Remove noise, garbage text, and irrelevant content
- Do NOT guess missing values
- If a field is unclear → return "$"
- Output MUST be strictly valid JSON
- No trailing commas, no markdown

Expected fields (if present):
- name
- dob
- gender
- id_number (aadhaar/pan/passport/dl)
- father_name
- address

Return JSON in this format:
{
  "aadhaar": {...},
  "pan": {...},
  "driving_license": {...},
  "passport": {...}
}

INPUT:
{documents_section}

OUTPUT: