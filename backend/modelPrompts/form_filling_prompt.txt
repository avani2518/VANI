Fill the given form JSON using extracted document data.

Rules:
- Use only provided data
- No guessing; if missing → "$"
- If conflict → "$"
- Priority: Aadhaar > Passport > Driving License > PAN
- Keep exact JSON structure
- Output MUST be strictly valid JSON
- Do NOT add trailing commas
- Do NOT add comments or explanations
- Do NOT wrap in markdown (no ```)

Allowed:
- Split address into fields if clear
- Normalize formatting

update_type:
- "yes" if field exists in data, else "$"

IMPORTANT:
- Ensure JSON is parseable using standard JSON parsers
- Every object and array must follow strict JSON syntax

INPUT:
FORM:
{form_structure}

DATA:
{extracted_data}

OUTPUT: