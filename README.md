# VANI
installations{

    pip install flask opencv-python pillow spacy transformers
    pip install easyocr
    pip install python-dotenv
    pip install openai
    pip install paddlepaddle paddleocr
    pip uninstall paddleocr paddlex paddlepaddle -y
    pip install rapidocr-onnxruntime
}